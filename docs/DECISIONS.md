# Architecture Decision Records (ADR Log)

Chronological log of key decisions. Newest decisions appended at the bottom.

| # | Decision | Rationale |
|---|---|---|
| 1 | Backend is Python + FastAPI only (no Node hybrid for v1) | Native OpenAPI, strong for parsing/LLM/data; single runtime simplifies deploy/auth/testing. Modular so a worker can split later. |
| 2 | Frontend: React + Vite + `@ui5/webcomponents-react` | Authentic SAP Fiori look (T.11) with React productivity. |
| 3 | Auth: Keycloak (OIDC) with RBAC (Owner/Editor/Viewer) | Standard, SSO-ready; multi-user requirement. |
| 4 | Keycloak shares the same PostgreSQL instance (separate schema) | One DB container; simpler ops. |
| 5 | Events: transactional outbox table only (CloudEvents 1.0); broker optional/future | Minimum viable per user choice; guarantees data/event consistency. |
| 6 | mnemonic IDs immutable; prefix change affects only NEW records | Preserves referential stability (A.1). |
| 7 | Per-prefix configurable pad width in `id_sequence` | Supports TRN 10-digit vs PRT 5-digit (A.2). |
| 8 | Cross-currency transfers store both leg amounts + derived fx_rate | Correct multi-currency accounting (A.4). |
| 9 | Investment current value = latest `valuation_history` row (cache field allowed) | Single source of truth (A.5). |
| 10 | SQL reporting: read-only role, timeout, forced LIMIT, views only, no DDL/DML | Security for free-form SQL (6.2/A.6). |
| 11 | T.9 confirmation enforced at UI only; APIs use prepare/commit for destructive bulk ops | Keeps APIs clean/automatable (A.7). |
| 12 | CloudEvents emitted only for state-changing operations | Avoids read noise (A.8). |
| 13 | Configurable holiday calendars for business-day recurrence rules | Supports "last working day" (A.9). |
| 14 | Document provenance stores original filename + server storage key (not client full path) | Browser cannot obtain client full path (A.10). |
| 15 | Expense item fulfilled by multiple transactions; `expense_item_seq_no` auto-assigned but editable | Matches A.11 semantics. |
| 16 | Category inheritance = Policy 1: item-linked transactions inherit item's category; no category-split when item-linked | Integrity between transaction & item category (D.a). |
| 17 | Income & expense unified in one `cash_flow_item` table with `flow_type` flag | Reuses recurrence/budget logic; supports projections. |
| 18 | `integration_endpoint` is a single registry for all non-LLM integrations (FX/stock/crypto/SMTP) | Canonical, swappable interfaces (T.4.2). |
| 19 | LLM Gateway: central provider list, per-feature primary/secondary, disable switches, master switch, PII redaction, graceful placeholders | T.4.1 requirements. |
| 20 | Notifications: in-app always; email when SMTP configured | User choice; graceful degradation. |
| 21 | Soft delete for financial records; audit_log with before/after; correlation to CloudEvent id | Auditability & recoverability. |
| 22 | Commit continuously; attempt push to remote; if push fails, keep committing locally | User instruction. |
| 23 | All enumerated value sets are configurable entities (`code_list` + `code_value`); every former enum column becomes a `*_cv_id` FK to `code_value` constrained to a specific `list_key` | Drives value helps/comboboxes and server-side validation; lets users extend value sets. System lists seeded with predefined values. Structural `level` fields excepted. |
| 24 | Bundle a local **Ollama** container as a default, configurable LLM provider (kind=ollama) | Out-of-the-box, privacy-friendly, offline-capable local LLM; seeded as an `llm_provider` the Gateway can use as primary/secondary. Model pulled on demand; GPU optional. |
| 25 | Every list screen + `GET /api/v1/{entity}` supports free-text search, structured filters, sort, pagination (Fiori FilterBar; `pg_trgm` indexes) | Users can search/filter transactions, partners, beneficiaries, categories, etc. (spec 6.1 + user request). |
| 26 | `currency_rate` uses validity periods (`begin_date`/`end_date`), lookup `begin_date <= date < end_date`; open-ended entry uses `9999-12-31`; overlaps prevented by GiST exclusion constraint | User-requested; guarantees a valid rate always exists; replaces closest-date approach. |
| 27 | Reporting currency is configurable, **default USD** (`app_config.default_base_currency=USD`, per-user `app_user.base_currency`); transactions stay in native currency (mostly AED). Imports always land on the validation screen for user review/entry before recording | Reports (cash position, projection, net worth, volumes) roll up in USD via validity-period FX; user confirms/edits parsed statement rows before commit (spec 3.2). |
| 28 | `institution` is a configurable entity with a **`country` FK** (also a configurable ISO entity); `account.institution` free-text replaced by `account.institution_id` FK; `institution_type` added as a code list | User request; consistent value help/validation, country grouping, and reporting by institution/country. |
| 29 | Full **data export to XLSX**: single multi-tab workbook (download) or one file per entity written to a server folder | User request; covers config + master + transactional data. Browser cannot pick a client folder, so single-workbook download is the primary UX; folder mode targets a mounted server volume. |
| 30 | Add **DB admin GUI** dev utilities: **Adminer** (`:8081`) plus the existing MinIO console (`:9001`) for object storage | User request; convenient admin access to the databases in dev. Dev-only utilities (not intended for production exposure). (pgAdmin was included initially but removed to reduce image footprint; Adminer covers dev needs.) |
| 31 | **Round-trip import** from an export workbook (`POST /api/v1/import/xlsx`): destructive wipe-and-reload of every entity, preserving original UUID PKs | Enables porting content into a clean instance. Feasible because export preserves UUID keys and portable value encodings. Load runs in one transaction with constraints deferred (self-ref/circular FKs: beneficiary/expense_category parent, transaction↔transfer_group); cells are coerced back to column types (UUID/date/Decimal/JSON), `""`→NULL; Decimals reconstructed via `Decimal(str(...))` to avoid float artifacts. Delete order is the reverse of insert order. UI requires an explicit confirm step. |
| 32 | Frontend uses a **metadata-driven CRUD layer** (`entities.js` + reusable `EntityManager`/`EntityForm`/`ComboField`/`ConfirmDialog`) giving every master/config/transactional entity create/view/edit/delete with autocomplete value help; **every state-changing action requires an explicit confirmation dialog** (extends ADR #11/#T.9 to all UI writes) | Avoids per-entity form duplication; consistent Fiori UX; satisfies universal confirm-on-write requirement. |
| 33 | **Transaction splits API** (`GET/POST/PATCH/DELETE /api/v1/transactions/{id}/splits`): split amounts must sum exactly to the transaction amount; sets `is_split=true`; disallowed when the transaction is linked to a `cash_flow_item` (Policy 1, ADR #16) | Enables multi-category splitting from the UI; enforces integrity server-side. |
| 34 | **Code-value (and code-list) admin API** (`POST/PATCH/DELETE /api/v1/code-lists/{key}/values`): create/edit/deactivate values, honoring `code_list.is_system` and `allow_user_values` guards (system-locked lists reject user edits) | Config data must be maintainable from the UI (ADR #23) while protecting seeded system lists. |
| 35 | **Partner gains `country_id`** (FK country); import mapping will match/create suppliers with country awareness | User request; consistent value help + country-based reporting/mapping for partners. |
| 36 | **Loan gains a configurable `loan_category`** code list (`loan_category_cv_id` FK to code_value: mortgage/personal/car/student/business/other) | User request; classify loans for reporting. |
| 37 | **Structured list filters**: every list screen has a filter bar (`FilterBar` + per-entity `filterFields`); `build_crud_router` accepts declared `filter_fields` as exact-match query params (extends ADR #25 beyond search to structured filters on generic entities) | User request; transactions already had rich filters, now all lists do. |
| 38 | **Refined confirmation model** (supersedes the create/edit-confirm part of ADR #32): **no confirmation on Save**; confirm on **Cancel only when the form is dirty**, on **Delete**, and on **complex/destructive actions** (import commit, import cancel) | User request; reduces friction while still guarding data loss and destructive ops. |
| 39 | **Additive schema convergence at startup**: `bootstrap._apply_additive_columns()` runs idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for new nullable columns, so existing databases gain new columns without a destructive reset (create_all/initial migration only create tables) | Avoids losing data on dev volumes when models evolve; complements the Alembic-preferred flow. |
| 40 | **Frontend routing via HashRouter**; confirm dialogs are rendered as **siblings** (never nested inside another Dialog, which blocked clicks) | Reliability fixes found during UX testing (Phase 11). |
| 41 | **UI framework migrated from UI5 Web Components to MUI (Material UI, Community/MIT)** with a finance-blue theme (supersedes ADR #2). Lists use MUI **DataGrid** (built-in column filters/sort/pagination); value help uses MUI **Autocomplete** (type-ahead); dates use MUI **X Date Pickers**; charts use **Recharts**. Metadata-driven CRUD layer (`entities.js` + `EntityManager`/`EntityForm`/`ComboField`/`ConfirmDialog`/`SplitEditor`/`FilterBar`/`CodeValueManager`) re-implemented on MUI | UI5 v1/v2 API churn and unfriendly comboboxes; MUI Community is free (only MUI X Pro/Premium is paid, not used) and speeds up admin-style CRUD. |
| 42 | **Audit `_snapshot()` is JSON-safe** (coerces Decimal→float, UUID/date→str) | Writing Numeric columns to `audit_log` JSONB raised HTTP 500 on Account/CashFlowItem/CurrencyRate create. |
| 43 | **Hierarchical `level` is derived server-side** (`parent.level+1`, else 1; depth guard beneficiary 2 / expense_category 3; self/cycle rejected) via a `pre_write` hook on the CRUD factory; the manual Level field is removed from forms | User feedback: level should follow the parent automatically, not be entered. |
| 44 | **Full ISO seed data** (`iso_data.py`: ISO 4217 currencies + ISO 3166-1 countries) replaces the small starter lists | User request for complete currency/country lists. |
| 45 | **Startup init is serialized with a Postgres advisory lock** (API + worker) and new nullable columns are added idempotently (`ADD COLUMN IF NOT EXISTS`) | Concurrent seeding across containers caused duplicate-key races / backend exit 3; existing dev DBs must gain new columns without reset. |
| 46 | **Cash-flow-item → transaction via an item action** (not a field on the transaction form): the transaction form no longer has a `cash_flow_item_id` field; instead a per-row "Create transaction" action on Cash Flow Items materializes a transaction (`POST /v1/recurring/materialize`) that inherits the item's category and is non-split (Policy 1). Standalone transactions keep free category + splits | User request; removes the confusing category/split conflict from the transaction form and makes the item→transaction link explicit. |
| 47 | **Budget lines UI** (`BudgetLinesDialog` via a per-row "Manage lines" action on Budgets) using the existing `/v1/budgets/{id}/lines` + `/variance` endpoints; **expense-category hierarchy seeded from a bundled CSV** (`seed_expense_categories.csv`) on first install (only when the table is empty; parents resolved by name, level derived) | #6 (budgets need lines) and #8 (default categories on first install). |
| 48 | **Admin/settings API** (`app/api/admin.py`): `app_config` key/value CRUD (`GET /v1/app-config`, `PUT /v1/app-config/{key}`) incl. the **LLM master switch** (`llm.enabled`); **entity prefixes** (`GET /v1/id-sequences`, `PATCH /v1/id-sequences/{prefix}` pad width 1..18); **current-user profile** (`GET/PUT /v1/profile`). `app_user` gains `name` + display-format prefs (`date_format`/`number_format`/`time_format`, additive). Frontend **Settings** page (App Settings + Entity Prefixes + My Profile) and a **My Profile** route from the avatar menu | A.7 (settings incl. LLM master switch), user profile, and entity-prefix maintenance. |
| 49 | **Holiday calendar weekend/week-start + day editor** (A.1): `holiday_calendar` gains `weekend_days` (JSONB list of weekday ints) + `week_start` (additive); new `DELETE /v1/holiday-calendars/{id}/days/{day_id}`; frontend `HolidayDaysDialog` (row action on Configuration) manages explicit holidays + recurring weekend + week-start | User request; recurring weekends plus explicit holiday dates drive business-day recurrence rules. |
| 50 | **Auto-create a backing Account for loans/investments** (A.6): a shared `ensure_backing_account()` helper wired via the CRUD `pre_write` hook on the loan and investment routers creates a dedicated account (name "… (Loan/Investment account)", zero opening balance, entity currency) when no `account_id` is supplied on create | User request; loans/investments need somewhere for balances/transactions to live without manual account setup. |
| 51 | **Country-aware import mapping** (#4): the parser accepts an optional `country` (ISO2/3) that biases date-format order (US = month-first, else day-first) and decimal separator (comma-decimal locales like DE/FR/…); threaded through `POST /v1/imports` as a `country` form field and a country selector on the Imports upload step | User request; statements from different countries use different date/number conventions. |
| 52 | **More reports** (#13): `monthly_trend()` reporting service + `GET /v1/reports/monthly-trend` (income vs. expense per YYYY-MM in reporting ccy; direction from linked item's flow_type else amount sign); Reports page gains a **Monthly Trend** line chart and a **Budget vs. Actual** bar chart (per-budget picker on the existing `/variance` endpoint) | User request; projection/budget-vs-actual/monthly-trend visualizations. |
| 53 | **Installment/loan payment tracking** (#15/#16): `POST /v1/installment-plans/{id}/schedule/{sid}/pay` (creates a linked transaction with `installment_plan_id`, marks the schedule row `paid`) and `POST /v1/loans/{id}/schedule/{sid}/pay` (creates a linked transaction for principal+interest, sets `linked_txn_id`); default to the plan/loan's account when none is given. Frontend `ScheduleDialog` (generic over installment/loan via `kind`) opened by a per-row "Schedule & payments" action on the Loans and Installments lists — generate schedule + record payments with a paid indicator | User request; track which installments/loan periods have been paid and link them to transactions. |
| 54 | **Investment valuation history UI** (#18): frontend `ValuationDialog` (per-row "Valuation history" action on Investments) over the existing `GET/POST /v1/investments/{id}/valuations` + `POST …/refresh-valuation` — a trend line chart, a history table, manual add, and refresh-from-source | User request; view/maintain investment valuations and see the trend. |
| 55 | **Recurring cash-flow items UI** (#19): new **Recurring** page lists pending occurrences (`GET /v1/recurring/pending?until=`) for cash-flow items linked to a recurrence profile whose due dates have no transaction yet, and materializes any into a transaction (reuses `MaterializeDialog` → `POST /v1/recurring/materialize`). `recurrence-profiles` added as a managed entity (Configuration) and `cash-flow-items` gained a `recurrence_profile_id` field to link a schedule | User request; surface upcoming recurring obligations and turn them into transactions. |
| 56 | **Transfers dialog** (A.5): frontend `TransferDialog` (a "New Transfer" action on the Transactions page) posts to the existing `POST /v1/transfers`, creating a dual-leg transfer (two linked transactions + a `transfer_group`); supports cross-currency by entering the received amount/currency on the other leg | User request; move money between accounts without hand-building two transactions. |
| 57 | **In-app Help/Wiki** (A.2): static `Help` page (Help nav entry) with collapsible sections covering getting-started, **categorization rules** (priority/conditions/actions JSON), cash-flow items & Policy 1, recurring items, transfers, imports (country-aware), reporting currency & FX, and settings/profile | User request; onboarding + concept reference without leaving the app. |
| 58 | **Auth: default admin user + Users admin + password fallback** (#14): the Keycloak realm now seeds a default **admin** user (password `admin`, Owner role) and enables the client's **direct-access grant**; backend `admin.py` adds a **Users admin API** (`GET/POST /v1/users`, `PATCH /v1/users/{id}`, `POST/DELETE /v1/users/{id}/roles`) over the local `app_user`/`role`/`user_role` tables and a **password-login proxy** (`POST /v1/auth/password-login`) to Keycloak's token endpoint; frontend adds a **Users** admin screen, a **"Sign in with password"** dialog, and a fallback token in `auth.js` (sessionStorage) so `getToken`/`getUser` work without the SSO redirect | User request; provide a first-login admin, in-app user/role management, and a password login that doesn't require the full OIDC redirect. Default credentials must be changed in Keycloak for any real use. |
| 59 | **UX fix pass (post-Phase-11 feedback):** (a) materialize now sets txn **direction from the item's flow_type** (income→credit, expense→debit); (b) the transaction form gains a **disabled `cash_flow_item` field** and, when a transaction is item-linked, **category + direction are inherited and locked** (new `lockWhenItemLinked`/`disabled` field flags honored by `EntityForm`); (c) **direction is now mandatory** on transactions; (d) fixed the splits `PUT` (only sync when there are lines or clearing a previously-split txn — avoids the `api.put`/"put is not a function" path for plain txns); (e) **SplitEditor hides the Total/Remaining line when there are no split lines**; (f) **all monetary values render at 2 decimals** (DataGrid `money` columns + a `money2` helper in Reports); (g) renamed "Volume by Supplier" → **"Volume by Partner"**; (h) **Keycloak admin console** exposed as a dev utility (proxy `/auth/admin` and direct `:8082`) | User-reported issues after the feature pass. |
| 60 | **Hot-reload dev workflow** via `infra/docker-compose.override.yml` (auto-merged by Compose): the **frontend** runs the **Vite dev server** (Dockerfile `build` stage; source bind-mounted; anonymous volume preserves image `node_modules`) on port 80 behind Traefik with HMR; the **backend/worker** run with `uvicorn --reload` and the source bind-mounted. `vite.config.js` sets `host`/`port:80`/`allowedHosts`/`hmr.clientPort` for behind-proxy dev. Edits now show up on a browser refresh (frontend) or automatically (backend) without rebuilding images; the production-style build still runs via `docker compose -f docker-compose.yml up -d --build` | The prod images bake the frontend bundle at build time, so a browser refresh alone never picks up code changes; a first-class dev workflow avoids per-change rebuilds. |
| 61 | **Keycloak initial-admin env vars** (fix): Keycloak **25.x** creates the master admin from the legacy **`KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD`** (the `KC_BOOTSTRAP_ADMIN_*` names are 26.x and were silently ignored, so no admin existed). Compose now sets **both** name pairs. The initial admin is only created when the master realm doesn't exist, and the realm import only runs on a fresh realm — so applying #14/#58 to an existing dev DB requires wiping the `keycloak` schema (`DROP SCHEMA keycloak CASCADE; CREATE SCHEMA keycloak`) and restarting Keycloak; app data in the `pfm` schema is untouched | The user couldn't log in to the Keycloak console; root cause was the 26.x-only env var names on a 25.x image plus import-skip on an existing realm. Verified: master admin token 200 and app password-login (`admin`/`admin`, role Owner) 200. |
| 62 | **Fix SSO redirect loop on the home screen** (`auth.js`): Keycloak is now initialized **without `onLoad:"check-sso"`** — the silent-SSO iframe/redirect flow looped behind the dev proxy (HashRouter leaves `state`/`code` on the base URL and the session check re-triggers, changing `state` each reload). We only complete a login when the URL already carries a real OIDC callback (`code`+`state`); otherwise the app loads as guest and the user signs in explicitly (SSO button or password dialog). Leftover OIDC params are stripped via `history.replaceState`, and SSO init is skipped entirely when a password-fallback token exists. **Also**: `vite.config.js` enables `server.watch.usePolling` because bind-mounted source on Windows/Docker Desktop emits no inotify events — without it the dev server kept serving a **stale transformed** module, so earlier fixes never reached the browser | User saw the Overview page continuously reload with a changing `state` param even in incognito; two causes — the auto `check-sso` redirect flow, and Vite HMR not detecting bind-mount edits. |
| 63 | **[Session 742, Batch 1] Profile row auto-provision by Keycloak subject (Bug 1):** `app_user` gains a nullable, indexed **`keycloak_subject`** column (additive). `admin.py::_get_or_create_user` resolves the caller by `keycloak_subject` → legacy `uuid==sub` → `email`, and if still not found **creates** the `app_user` (name/email from the token) and backfills the subject link. Fixes the "404: No user profile row for the current identity yet." error when an admin (or any signed-in user) edits their profile | The profile lookup never created a row and matched only by `sub==uuid` or exact email, so Keycloak users got a 404 on save. |
| 64 | **[Session 742, Batch 1] Full Keycloak user provisioning (Bug 2):** new `app/services/keycloak_admin.py` (httpx) obtains a **master-realm admin token** (`admin-cli`, from `KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD`, now also backend settings) and provides `create_user`/`set_password`/`assign_realm_role`/`remove_realm_role`/`refresh_token`. `POST /v1/users` now takes a **username** (+ optional name/email/role/password), creates the Keycloak user with a **temporary password** (auto-generated if omitted, returned once as `temp_password`), assigns the realm role, and mirrors an `app_user` keyed by `keycloak_subject`; role grant/revoke also sync to Keycloak. `user_role.grant_household_id` is made **nullable** (idempotent `ALTER ... DROP NOT NULL` in bootstrap) — the previous `grant_household_id=role.uuid` (a role id, not a household) plus NOT NULL was the source of the HTTP 500. Keycloak errors surface as 422 with detail | Adding a user created only a local mirror (no password, no real identity) and role grants 500'd on the bogus non-null household id. |
| 65 | **[Session 742, Batch 1] Password-fallback token refresh (Bug 3):** the password-login session now persists the **refresh token** (`auth.js`, sessionStorage) and exposes `refreshFallback`/`hasFallbackSession`/`clearFallbackSession`; new backend `POST /v1/auth/refresh` proxies Keycloak's refresh grant. `api.js` transparently **refreshes once + retries on a 401**, and if that still fails it clears the session and dispatches a `pfm:session-expired` event; `App.jsx` listens for it and opens the login dialog with a "session expired" notice instead of cascading raw 401s on every page | The fallback token was never refreshed, so once the short-lived access token expired every navigation showed "401: Invalid token: Signature has expired" until logout. |
| 66 | **[Session 742, Batch 2] Account Type + Institution mandatory (Bug 4):** `account_type_cv_id` and `institution_id` are now **required** on `AccountCreate` (422 if missing) and `required: true` in the accounts form | User request; accounts must be classified and attributed to an institution. |
| 67 | **[Session 742, Batch 2] Loan/investment always auto-create a backing account (Bug 10):** `ensure_backing_account()` now runs unconditionally on create and tags the account with the right `account_type` code (loan→`loan`, investment→`investment`); the loan/investment create forms show the **Account field disabled** with an explanatory note (a backing account is created automatically). Confirmed the wiring fires for both entities | User request; loans/investments should always get a dedicated backing account without manual setup, and the loan account should be a loan-type account. |
| 68 | **[Session 742, Batch 2] Installment↔transaction link surfaced (Bug 11):** the link is `installment_schedule.linked_txn_id` + `transaction.installment_plan_id`, created by **Record payment** in `ScheduleDialog`; the dialog now documents this and the paid indicator reflects it | User asked where the schedule dialog is and how installments link to transactions. |
| 69 | **[Session 742, Batch 2] Budget line either/or (Bug 12) + label resolution (Bug 22):** a budget line is **either** a Cash Flow Item (category + direction inherited server-side; category/direction inputs hidden) **or** a Category+Direction pair — enforced in `add_budget_line` (422 otherwise) and via a mode toggle in `BudgetLinesDialog`, which also resolves Category/CFI/Direction **UUIDs → labels** in the table | User request; a cash-flow item already carries its category & direction, so entering all three was redundant. |
| 70 | **[Session 742, Batch 2] Goals ↔ transactions (Bug 19) + CSV schedule import (New-1):** `goal` gains `goal_type_cv_id` (new `goal_type` code list: `save_to_target`/`cap_expense`), `expense_category_id`, `period`, `limit_amount`; `transaction` gains `goal_id` (all additive). New `GET /v1/goals/{id}/progress` evaluates a goal against transactions (save-to-target: sum of goal-tagged txns vs target; cap-expense: category spend over the period vs limit — e.g. "fuel < 1000/month"). New `POST /v1/loans/{id}/schedule/import` and `POST /v1/installment-plans/{id}/schedule/import` accept a CSV (replace/append) parsed via stdlib `csv`; `ScheduleDialog` gains an **Import CSV** button. `EntityForm` gained a `select` field type for the goal period | User decisions: goals link to transactions and can be rule-style caps; user request: import loan/installment schedules from CSV. |
| 71 | **[Session 742, Batch 3] Reports name resolution + cash projection (Bugs 5, 22, 23):** `reporting.volume_by_field` now resolves partner/beneficiary FKs to a **`label`** (name; null→"Unassigned") so the Volume-by-Partner/Beneficiary pies show names, not UUIDs/"—" (Bug 5). Budget-line UUID→label resolution shipped in Batch 2's `BudgetLinesDialog` (Bug 22). New **cash-projection** service + `GET /v1/reports/cash-projection?budget_id=&months=` computes **month-end cash/investments/loans/net** for N months (start = current cash + investments + outstanding loan balances; each month applies the budget's signed net flow to cash and draws loan balance down by that month's scheduled principal from generated amortization schedules); Reports page gains a **Cash Projection** multi-line chart with a Budget picker + months input (Bug 23) | User request; the volume legends were unreadable and there was no month-by-month cash/net projection. |
| 72 | **[Session 742, Batch 4] Settings/SMTP/FX/LLM (Bugs 6, 7, 8, 20, New-2):** (6/7) standardized the LLM master switch on **`llm.master_enabled`** — Settings uses it and the seeder **migrates + deletes** the stray `llm.enabled` row; documented as the global kill-switch. (8) **rewrote the LLM gateway** (`services/llm_gateway.py` had been a duplicate of the rules engine and lacked `complete()`, so budget recommendations would have crashed) as a real gateway that iterates `llm_provider` by **`params.priority`** ascending with failover, gated by `llm.master_enabled`; **New-2** exposes a flat **Priority** field on the LLM Providers form (folded into `params.priority` via a `pre_write`). (8) consolidated SMTP onto **discrete `smtp.*` app-config keys** (host/port/security[none\|starttls\|ssl]/username/password/from/to), added `send_test_email` + `POST /v1/notifications/test-email`, and a generic **Email (SMTP)** settings card with a **Send test email** button (Help/example: Yahoo). (20) **"Refresh from source"** card on Configuration → Currency Rates posts to the existing `POST /v1/fx/refresh` per quote currency | User-reported issues; also fixed a latent crash in the LLM gateway and removed the duplicate SMTP scheme. |
| 73 | **[Session 742, Batch 5] Investment valuation refresh fix:** `valuation.refresh_holding(db, holding, on=None)` now takes a **target date** (default today), **overwrites** the row for that date (else inserts), and keeps `current_value_cache` on the latest-dated valuation. It raises a typed **`ValuationError`** — `kind="manual_only"` for asset types with no price source, `kind="source"` for unreachable/unknown-symbol — which `POST /v1/investments/{id}/refresh-valuation` (now accepting an `on` body param) surfaces as a **422 with a helpful message** (instead of the opaque "Could not fetch a price"). `ValuationDialog` passes the chosen **As of** date to refresh and explains manual-only assets | The refresh button always 422'd for manual/generic assets and ignored the chosen date; users couldn't tell why. |
| 74 | **[Session 742, Batch 6] Import intelligence & flexibility (Bugs 17, 18):** new **`import_mapping_memory`** table (`source_text → mapped_partner_id/mapped_category_id`, `accept_count`) learns from accepted commits — `commit_import` upserts+increments per row, and `import_mapper.map_row` recommends the **most-frequently-accepted** mapping for a row's statement text (bank supplier text is stable). **LLM-assisted** categorization added as a soft, gated (`llm.master_enabled`) hint (`suggested_category_name`) that never blocks import. **Per-row account (Bug 18):** `map_row` deduces an `account_id` from `account`/`iban`/`account_number` columns (guarded by `hasattr` since `account` lacks iban/number columns today), and `commit_import` books each row to its deduced account, falling back to the commit's default account | User request; learn supplier→category from user actions, optionally use the LLM, and support a generic multi-account CSV. |
